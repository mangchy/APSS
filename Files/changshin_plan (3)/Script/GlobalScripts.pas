#language PascalScript

  //auto-generated

var
    TimerLoadProgram : TTimer;
    TimeCheckLoadProgram  : TdhTimeCheck;
    Timer1 : TTimer;
    TimeCheck1  : TdhTimeCheck;
    Timer2 : TTimer;
    TimeCheck2  : TdhTimeCheck;
    Timer3 : TTimer;
    TimeCheck3  : TdhTimeCheck;
    TimerGetWorkCount : TTimer;
    TimeCheckGetWorkCount  : TdhTimeCheck;
    TimerFinishWork : TTimer;
    TimeCheckFinishWork  : TdhTimeCheck;
    TimerInitWorkCount : TTimer;
    TimeCheckInitWorkCount  : TdhTimeCheck;
    TimerKResponse : TTimer;
    TimeCheckKResponse  : TdhTimeCheck;
    TimerScreenDownload : TTimer;
    TimeCheckScreenDownload  : TdhTimeCheck;


begin
end.
