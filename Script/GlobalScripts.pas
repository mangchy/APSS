#language PascalScript

  //auto-generated

var
    TimerDateTime : TTimer;
    TimeCheckDateTime  : TdhTimeCheck;
    TimerDownload : TTimer;
    TimeCheckDownload  : TdhTimeCheck;
    TimerDownloadWork : TTimer;
    TimeCheckDownloadWork  : TdhTimeCheck;
    TimerGetAct : TTimer;
    TimeCheckGetAct  : TdhTimeCheck;
    TimerCheck : TTimer;
    TimeCheckCheck  : TdhTimeCheck;
    TimerAllClear : TTimer;
    TimeCheckAllClear  : TdhTimeCheck;
    TimerInitOrder : TTimer;
    TimeCheckInitOrder  : TdhTimeCheck;
    TimerRepaintGrid : TTimer;
    TimeCheckRepaintGrid  : TdhTimeCheck;
    TimerReadBarcode : TTimer;
    TimeCheckReadBarcode  : TdhTimeCheck;
    TimerSyncTimeLP : TTimer;
    TimeCheckSyncTimeLP  : TdhTimeCheck;
    TimerRefresh : TTimer;
    TimeCheckRefresh  : TdhTimeCheck;


begin
end.
